# @astrouse/docs

## pnpm

```
pnpm add -D <package_name> ... -w

pnpm add <package_name> ... --filter package-project-dir

pnpm add --save-peer <package_name> ... --filter package-project-dir

pnpm --filter <package_name> dev

pnpm --recursive update
```
